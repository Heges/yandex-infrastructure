---
apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.global.backendPort }}
      protocol: TCP
      targetPort: {{ .Values.global.backendPort }}
  selector:
    app: backend 